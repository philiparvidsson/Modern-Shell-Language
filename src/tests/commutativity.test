name="commutativity"
function assert(evaluated, expected) {
    return evaluated==expected
}

a = 2*(3 + 4)
b = (4 + 3)*2

bool = assert(a,14)
bool = bool * assert(b,14)

if (bool==0) print(name, "failed.")